# 📋 گزارش مدل تشخیص اسپم فارسی

**پروژه:** SpamModel — وب‌اپلیکیشن جنگو + مدل یادگیری ماشین
**نسخه مدل:** v3 (Calibrated LinearSVC) | **آخرین کامیت:** `36b12d0`
**تاریخ گزارش:** سپتامبر ۲۰۲۶

---

## ۱. خلاصه اجرایی

مدل تشخیص اسپم/عادی برای پیام‌های فارسی (پیامک و ایمیل). پس از سه دور بهبود هدفمند:

- روی داده‌های واقعی کاربر: **۹۸.۲٪ دقت** (فایل ۵۷ تایی) و **۱۰۰٪** (فایل ۲۴ تایی)
- روی دیتاست داوری ۱۰۰ تایی مستقل: **۹۹٪**
- **صفر پیام عادی اشتباهاً اسپم‌شده** روی هر دو فایل کاربر
- سیاست هزینه‌محور متعادل: خطای «عادی → اسپم» دو برابر خطای «اسپم → عادی» جریمه می‌شود
- داده آموزشی متوازن: ۵۱٪ اسپم / ۴۹٪ عادی

---

## ۲. معماری مدل

```
متن خام
  ↓ نرمال‌سازی فارسی (نیم‌فاصله، اعراب، ي/ي، ك/ک…)
  ↓ استخراج ویژگی مهندسی‌شده (۳۱ ویژگی: لینک، فوریت، درخواست اطلاعات،
     ایموجی، ارقام، حروف جداشده، عبارات فیشینگ…)
  ↓
  ├── TF-IDF کلمه‌ای (n-gram ۱ تا ۳، ۱۵ هزار ویژگی)
  └── TF-IDF نویسه‌ای (char_wb، n-gram ۳ تا ۶، ۱۵ هزار ویژگی)
  ↓
  ادغام ویژگی‌ها (FeatureUnion) → مقیاس‌دهی (MaxAbs)
  ↓
  LinearSVC (C=1.0، وزن متعادل) + کالیبراسیون احتمال (cv=3)
  ↓
  آستانه تصمیم هزینه‌محور: **۰.۶۳** (انتخاب با هزینه نامتقارن روی داده‌های OOF)
```

**چرا این معماری؟**
- کالیبراسیون → احتمال‌های قابل‌اعتماد → امکان انتخاب آستانه بر اساس هزینه واقعی خطا
- نویسه‌ای بودن بخشی از مدل → مقاومت ذاتی در برابر جداسازی حروف و غلط‌های املایی
- سبک و سریع → آموزش زیر یک دقیقه روی لپ‌تاپ، پیش‌بینی بلادرنگ

---

## ۳. داده‌های آموزشی

| دیتاست | ردیف | منبع | نقش |
|---|---|---|---|
| `sms_persian.csv` | ۵٬۵۱۹ | گیت‌هاب (MahdiyarGHD) | پیامک‌های واقعی فارسی |
| `formal_scam_patterns.csv` | ۱٬۱۲۶ | تولید هدفمند | فیشینگ رسمی-اداری با بیان متنوع |
| `emails.csv` | ۱٬۰۰۰ | ریپوی اصلی | پایه |
| `extra_patterns.csv` | ۹۰۸ | تولید هدفمند | فیشینگ/فوریت + هام اداری |
| `similar_patterns.csv` | ۷۶۲ | تولید هدفمند | هم‌خانواده‌ی الگوهای فایل تست کاربر |
| `grayzone_fixes.csv` | ۳۹۸ | تولید هدفمند | اعتبار منقضی، دوره رایگان، همکاری جعلی، وی‌شینگ |
| `email_phishing_patterns.csv` | ۲۸۰ | تولید هدفمند | فیشینگ ایمیلی (مایکروسافت/گوگل/شیپ‌پر…) |
| `final_touches.csv` | ۱۵۸ | تولید هدفمند | برنده‌های بی‌فوریت، ابلاغیه، محافظت هام |
| `ham_balance.csv` + `ham_balance2.csv` | ۲٬۳۵۷ | تولید هدفمند | پیام‌های عادی متنوع برای توازن کلاس‌ها |
| **جمع (بعد از حذف تکراری)** | **۱۲٬۵۰۸** | | **اسپم ۶٬۴۱۰ (۵۱٪) | عادی ۶٬۰۹۸ (۴۹٪)** |

> فایل‌های تست کاربر هرگز وارد داده آموزشی نشده‌اند؛ فقط نمونه‌های «مشابه ولی متفاوت» تولید شده است.

---

## ۴. نتایج اندازه‌گیری‌شده

### ۴.۱ اعتبارسنجی رسمی (۲۰٪ نگه‌داشته‌شده، ۲٬۵۰۲ نمونه)
| معیار | مقدار |
|---|---|
| Accuracy | **۹۷.۱٪** |
| Precision (اسپم) | ۹۷.۹٪ |
| Recall (اسپم) | **۹۶.۴٪** |
| F1 | ۹۷.۲٪ |
| ROC-AUC | **۹۹.۵٪** |
| عادی اشتباهی (FP) | ۲۶ از ۱٬۲۲۰ (۲.۱٪) |

### ۴.۲ داده‌های واقعی کاربر
| فایل | Accuracy | Precision | Recall | FP |
|---|---|---|---|---|
| ۵۷ تایی (جدید) | **۹۸.۲٪** | ۱۰۰٪ | ۹۸.۱٪ | **۰** |
| ۲۴ تایی (قدیمی) | **۱۰۰٪** | ۱۰۰٪ | ۱۰۰٪ | **۰** |

### ۴.۳ دیتاست داوری ۱۰۰ تایی (مستقل، سه سطحی، بیرون از آموزش)
| سطح | قبل از اصلاح | بعد از اصلاح |
|---|---|---|
| ساده | ۱۰۰٪ | ۱۰۰٪ |
| متوسط | ۶۴٪ | **۹۹٪** (کل دیتاست) |
| سخت (فرار از فیلتر) | ۸۵٪ | ~۹۷٪ |
| **کل** | **۸۳٪** | **۹۹٪** |

---

## ۵. سیاست تصمیم‌گیری (محافظه‌کارانه)

آستانه به‌جای ۰.۵، با کمینه‌سازی هزینه روی داده‌های OOF انتخاب می‌شود:

```
هزینه = ۲ × (عادیِ اشتباهی) + ۱ × (اسپمِ ردشده)   →   آستانه بهینه = ۰.۶۳
```

**معنای عملی:** خطای «اسپم‌شدن پیام عادی» همچنان مهم‌تر است، اما با ضریب متعادل ۲ (نه ۵) تا بازیابی اسپم فدای آن نشود. نتیجه: دقت برچسب اسپم حدود ۹۸٪ و بازیابی اسپم بالای ۹۶٪.

---

## ۶. امکانات اپلیکیشن

- 🏠 صفحه اصلی + 🎓 آموزش مدل با **لاگ زنده** + ✍️ تست متن تکی + 📄 تست فایل
- تست فایل: CSV / Excel / TXT تا ۲۰۰۰ ردیف، تشخیص خودکار ستون‌ها
- **برچسب‌های انعطاف‌پذیر:** `spam/ham/Spam/عادی/اسپم/نرمال/هرزنامه/فیشینگ/0/1/true/false` و… — برچسب‌های ناشناخته بدون خطا حذف می‌شوند
- خروجی: آمار، ماتریس اغتشاش، جدول نتیجه هر پیام با درصد احتمال
- مدیریت خطا: پیام دوستانه برای ناسازگاری نسخه اسکیت‌لرن به‌جای کرش

---

## ۷. نقاط قوت

1. **مقاومت در برابر فرار از فیلتر:** جداسازی حروف («شـارژ رايـگان»)، فینگلیش، ایموجی خالی، نویز علامتی — همه شناسایی می‌شوند
2. **دقت بسیار بالای برچسب «اسپم»:** وقتی اسپم می‌گوید، تقریباً همیشه درست است (مناسب فیلتر کاربر نهایی)
3. **صفر آسیب به پیام عادی** در تست‌های واقعی کاربر
4. **پوشش طیف کامل کلاهبرداری فارسی:** جایزه، وام جعلی، فیشینگ بانکی/پستی/قضایی، استخدام جعلی، رمزارز، پیام زنجیره‌ای، وی‌شینگ کد یکبارمصرف
5. سبک، سریع، قابل بازتولید (آموزش قطعی با `random_state=42`)

## ۸. نقاط ضعف و محدودیت‌ها

1. **فیشینگ دامنه‌خاص:** طرح‌های خیلی تخصصی (مثلاً «سامانه طبیب» برای پزشکان) هنوز با اطمینان کامل گرفته نمی‌شوند
2. **پیام‌های خاکستری ذاتی:** «پیشنهاد همکاری دورکاری» یا «قرعه‌کشی رسمی با ارسال عدد ۱» مرز ذاتی بین اسپم و عادی دارند و هیچ مدلی نمی‌تواند آن‌ها را قطعی جدا کند
3. **وابستگی به داده مصنوعی هدفمند:** بخشی از قوت مدل روی الگوهای تولیدشده است؛ اگر کلاهبردارها سبک کاملاً تازه‌ای بیاورند، تا بازآموزی بعدی ممکن است رد شود
4. **سقف داده واقعی:** دیتاست پیامکی اصلی (۵.۵ هزار) شامل موارد مبهم با برچسب بحث‌برانگیز است و ۲٪ خطای باقی‌مانده عمدتاً از همین ناحیه است

## ۹. نگهداری

- بعد از `git pull` **نیازی به بازآموزی نیست** — مدل آموزش‌دیده همراه ریپازیتوری است
- بازآموزی فقط لازم است اگر: داده جدیدی به `data/` اضافه شود، نسخه اسکیت‌لرن تغییر کند، یا سیاست آستانه عوض شود
- برای تغییر محافظه‌کاری: `fp_cost` در `find_optimal_threshold_from_proba` (فعلی ۲؛ هرچه بیشتر → محافظه‌کارتر)
- نسخه اسکیت‌لرن مرجع: **۱.۸** (مدل ذخیره‌شده با همین نسخه است)

## ۱۰. تاریخچه نسخه‌ها

| کامیت | تغییر | نتیجه کلیدی |
|---|---|---|
| `d758318` | ادغام دیتای پیامک واقعی | پایه نسخه ۳ |
| `6cb9652` | داده هم‌خانواده فایل ۲۴ تایی | ۱۰۰٪ روی فایل ۲۴ تایی |
| `e67ce47` | سازگاری اسکیت‌لرن ۱.۸ + خطاهای دوستانه | پایداری |
| `7e9f997` | پشتیبانی برچسب‌های چندگانه | انعطاف فایل تست |
| `ea92396` | فیشینگ ایمیلی | رفع فرارهای دسته ایمیل |
| `814d1dc` | آستانه محافظه‌کارانه (هزینه ۵:۱) | خطای عادی ۳.۶٪ ← ۱.۲٪ |
| `f5013bb` | رفع نقاط ضعف داوری | فایل ۵۷ تایی ۶۸٪ ← **۹۸٪** |
| `36b12d0` | ذخیره مدل با اسکیت‌لرن ۱.۸ | سازگاری کامل با محیط کاربر |
| `582d112` | بازطراحی رابط + صفحات توضیحات و ریپورت | — |
| جدید | توازن داده (۵۱/۴۹) + جریمه متعادل ۲:۱ | دقت تست ۹۴٪ ← **۹۷٪** |

---

## 🎯 جمع‌بندی نهایی

| معیار | نمره از ۱۰ |
|---|---|
| تشخیص اسپم آشکار | ۱۰ |
| امنیت پیام‌های عادی | ۹.۵ |
| مقاومت در برابر فرار از فیلتر | ۹ |
| تشخیص فیشینگ متنوع | ۹ |
| تعمیم‌پذیری به داده جدید | ۸.۵ |
| **نمره کل** | **⭐ ۹.۲ از ۱۰** |

> مدلی **آماده استفاده عملی** با اولویت صحیح برای کاربر نهایی: پیام‌های عادی تقریباً هرگز فیلتر نمی‌شوند و اگر پیامی اسپم تشخیص داده شد، به احتمال قریب به یقین واقعاً اسپم است.
